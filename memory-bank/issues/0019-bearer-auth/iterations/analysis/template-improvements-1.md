# Template Improvements 1

Из этого цикла стоит помнить:
- для ранних инфраструктурных issues шаблон плана должен явно разрешать placeholder runtime endpoints, если полный продуктовый endpoint вынесен в следующий issue;
- в run-инструкциях стоит указывать, когда локальная БД/порт недоступны из sandbox и нужен внешний прогон.
