# Brief v1

Нужно закрыть обязательный контракт C-6: без Bearer token любые будущие `/tasks*` endpoints должны отвечать `401`, а публичные технические endpoints не должны деградировать. После `#18` этого слоя нет вообще, поэтому `#20` не может безопасно строиться поверх текущего HTTP boundary.
