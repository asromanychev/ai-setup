# Draft Brief v1

Issue `#20` closes the gap between authenticated `/tasks*` boundary and the actual HTTP contract required by PRD v4. Without CRUD/lifecycle endpoints, neither upstream creation nor downstream consume/delete flows can happen through the service.
