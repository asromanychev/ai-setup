# Spec Review 1 — Issue #7

Результат: spec покрывает входные ветки (`collection_mode`, config shape, ENV), два runtime path (`polling`, `webhook`) и конфликт между PRD-checkpoint и `ChannelClient offset` через явную оговорку об internal `offset`.

Blockers: none.
