---
issue: 2
type: plan-review
reviewed: iterations/plan-v7.md (version: v7, status: draft)
review_number: 8
date: 2026-04-06
---

# Plan Review — Issue #0002 (plan-v7.md)

## Grounding

| Файл из плана | Статус |
|---|---|
| `core/lib/collect/errors.rb` | EXISTS; `CheckpointAmbiguityError` **отсутствует** → шаг 1 блокирует реализацию корректно |
| `app/models/application_record.rb` | EXISTS ✓ |
| `spec/spec_helper.rb` | EXISTS ✓ |
| `config/database.yml` | EXISTS ✓ |
| `db/` (каталог) | EXISTS ✓ |

## Критерии

### 1. Конкретность шагов
Каждый из 11 шагов называет конкретные файлы и единственное действие. Шаг 1 — измеримый prerequisite gate с условием остановки. Шаги 2–11 — атомарные дельты с явным указанием, что запрещено трогать.  
**Нет замечаний.**

### 2. Логика зависимостей
Порядок корректен: prerequisite gate (1) → миграции (2→3) → модели (4→5) → helper (6) → specs (7→8→9) → db:prepare (10) → runtime gate (11).  
Константа `SyncCheckpoint` в шаге 4 разрешается в Ruby runtime, не при загрузке класса; оба файла появятся до запуска тестов в шаге 11. Порядок безопасен.  
**Нет замечаний.**

### 3. Атомарность
Каждый шаг независимо выполним и проверяем. Шаги 7–9 — отдельные файлы или явные добавления в файл. Шаг 10 отделён от шага 11 и содержит только схему, без production-правок.  
**Нет замечаний.**

### 4. Полнота
Все 7 файлов из таблицы `spec.md §6` охвачены шагами 2–9. Все acceptance criteria из `spec.md §7` материализованы в шагах 7–9. Coverage gate явно упомянут в критерии завершения шага 11.  
**Нет замечаний.**

### 5. Grounding
Все существующие файлы, упомянутые в плане, реально существуют. Отсутствие `Collect::CheckpointAmbiguityError` в `core/lib/collect/errors.rb` наблюдаемо и корректно перехватывается gate-шагом 1.  
**Нет замечаний.**

### 6. Lifecycle placement
- `before_validation` нормализация → правильная фаза жизненного цикла ✓
- Guard `raise ArgumentError if position.nil?` — в runtime-методе до обращения к БД ✓
- `sync_checkpoint` reader — instance method с count-проверкой ✓
- FK и cascade — в миграции шага 3 ✓  
**Нет замечаний.**

### 7. Invariant materialization
Все пять инвариантов из `spec.md §5` материализованы в тестовые шаги:
- Уникальность пары → шаг 7 ✓
- Единственность checkpoint → шаг 8 (0/1 записи) + шаг 9 (ambiguity case) ✓
- Неизменность position после мутации аргумента → шаг 7 ✓
- Независимость checkpoint разных источников → шаг 7 ✓
- FK cascade при `source.destroy` → шаг 8 ✓  
**Нет замечаний.**

---

## Gate-вопросы

### Delta-first adequacy
Каждый шаг описывает конкретную дельту: что уже есть, чего не хватает, что именно добавляется. Ни один шаг не ограничивается описанием желаемого конечного состояния без указания точки отсчёта.  
**Нет замечаний.**

### Final runtime gate
Шаг 11 содержит две конкретные команды:
```
bundle exec rspec spec/models/source_spec.rb spec/models/sync_checkpoint_spec.rb
bundle exec rspec spec/core/collect
```
Критерий завершения явный: обе команды зелёные, coverage gate `core/lib` не деградирует. Внутри шага запрещены любые дополнительные правки.  
**Нет замечаний.**

---

## Проверка исправления из plan-review-7 (шаг 9)

Замечание 1 из plan-review-7: отсутствовало явное указание на `:db`-тег для ambiguity-spec, что допускало неопределённость в поведении cleanup.

**Исправление в v7:** шаг 9 теперь явно фиксирует:
1. Spec помечается тем же `:db`-тегом, что и примеры шагов 7–8.
2. В транзакционном контексте (`around(:each, :db)` из шага 6) откат транзакции очищает и вставленные данные, и DDL-изменения (DROP/CREATE INDEX) автоматически — загрязнение состояния исключено.
3. Блок `ensure` с `CREATE INDEX` остаётся как явная страховка на случай не-транзакционного запуска (отладка одиночного примера без `:db`-тега).

Замечание полностью устранено. ✓

---

## Итог

**0 замечаний, план готов к реализации.**
