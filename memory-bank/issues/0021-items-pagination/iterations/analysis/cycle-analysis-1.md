# Cycle Analysis 1

- Что сработало: issue лёг поверх уже готовых `#18/#19/#20`; существующий `TasksController` позволил добавить endpoint без новых моделей или миграций.
- Что пришлось скорректировать: первая версия теста на `EXPLAIN` опиралась на `Relation#explain`, но в текущем Rails это proxy; надёжнее проверять прямой `EXPLAIN #{relation.to_sql}`.
- Оставшийся риск: stable cursor гарантирован для повторного чтения при неизменном наборе items; параллельные новые вставки ожидаемо появятся только на следующих страницах с большим cursor, что соответствует контракту issue.
