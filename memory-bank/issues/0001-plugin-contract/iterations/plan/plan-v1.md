1. Целевые файлы: `core/lib/collect/plugin.rb`
   Суть изменения: довести базовый контракт `Collect::Plugin` до точного соответствия spec.md: явно сохранить и защищать `source_config` через глубокое копирование и глубокую заморозку, валидировать `checkpoint_in`, оставить `perform_sync_step` приватным и обязательным к переопределению, а также нормализовать результат `sync_step` в замороженный Hash с валидированными ключами `:records`, `:checkpoint_out`, `:finished`.
   Какие зависимости этот шаг разрешает: создаёт корректную базу для поведения всех субклассов и для последующих тестов контракта и `NullPlugin`, чтобы downstream-логика работала поверх уже зафиксированного интерфейса.

2. Целевые файлы: `core/lib/collect/plugin_registry.rb`
   Суть изменения: уточнить поведение `Collect::PluginRegistry` по спецификации: регистрация только классов с `.plugin_id`, проверка совпадения ключа регистрации с `plugin_class.plugin_id` после нормализации, сохранение эквивалентности Symbol/String через `.to_s`, а также подтверждение, что `build` делегирует в `fetch` и `.new`, а `default` регистрирует только `NullPlugin`.
   Какие зависимости этот шаг разрешает: фиксирует точку входа для построения плагинов, чтобы интеграционный сценарий через default registry и тесты ошибок реестра опирались на стабильную фабрику.

3. Целевые файлы: `core/lib/collect/null_plugin.rb`
   Суть изменения: привести `Collect::NullPlugin` к полному контракту спецификации: валидировать `source_config[:records]` при инициализации, валидировать `checkpoint_in[:cursor]` как неотрицательный Integer, трактовать отсутствие `:cursor` как `0`, на первом шаге эмитить исходный батч, на следующих шагах возвращать пустой `records`, и всегда формировать детерминированный `checkpoint_out` с `cursor` и `emitted_records`.
   Какие зависимости этот шаг разрешает: завершает рабочую тестовую реализацию плагина, необходимую для acceptance criteria по детерминизму, edge cases и интеграции через `PluginRegistry.default`.

4. Целевые файлы: `spec/core/collect/plugin_contract_spec.rb`
   Суть изменения: расширить unit-тесты базового контракта `Collect::Plugin`, чтобы покрыть все AC и инварианты для базового класса: ошибки на `source_config` и `checkpoint_in`, обязательность `.plugin_id` и `#perform_sync_step`, отсутствие мутаций входов, заморозку результата и вложенных структур, а также ошибки при неполном или неверно типизированном результате.
   Какие зависимости этот шаг разрешает: даёт автоматическую проверку корректности шага 1 и предотвращает регрессии контракта, на который опираются реестр и `NullPlugin`.

5. Целевые файлы: `spec/core/collect/plugin_registry_spec.rb`
   Суть изменения: дополнить unit-тесты `Collect::PluginRegistry` сценариями нормализации id, ошибкой для незарегистрированного плагина, отказом в регистрации класса без `.plugin_id`, отказом при несовпадающем `plugin_id`, а также проверкой `build` и `default`.
   Какие зависимости этот шаг разрешает: закрывает acceptance criteria для реестра и подтверждает, что orchestration через registry можно безопасно использовать в интеграционных тестах.

6. Целевые файлы: `spec/core/collect/null_plugin_spec.rb`, `spec/core/collect/registry_null_plugin_integration_spec.rb`
   Суть изменения: расширить тесты `Collect::NullPlugin` и интеграционный сценарий так, чтобы покрыть валидацию `source_config[:records]`, edge cases для `checkpoint_in` (`nil`, `{}`, строковый cursor, отрицательный cursor, cursor > 0), детерминизм, корректный `checkpoint_out`, а также end-to-end путь `PluginRegistry.default -> build("null") -> sync_step` без внешних зависимостей.
   Какие зависимости этот шаг разрешает: завершает проверку acceptance criteria на уровне unit и integration тестов и подтверждает, что покрытие `core/lib/` остаётся достаточным без добавления новых инфраструктурных зависимостей.
