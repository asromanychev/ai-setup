# Примеры промптов для запуска разработки через агента

## Настройка окружения

```
смотри есть такой https://github.com/sirmalloc/ccstatusline и я вроде как
подключил его в claude но как его правильно настроить чтобы видеть информацию
более подробную по контексту
```

> Агент прочитал существующий скрипт, диагностировал JSON-структуру через временный
> дамп, и переписал скрипт под реальные поля.

---

```
настрой мне строку ровно такую как я скинул на скрине
[скриншот с форматом: In: 15.2k | Out: 3.4k | Cached: 12k | ...]
```

> Промпт со скриншотом — агент распознал формат, сопоставил с полями JSON
> и реализовал точное воспроизведение.

---

## Диагностика проблем

```
а почему у меня claude не работает в другой папке
[скриншот с "zsh: command not found: claude"]
```

> Агент определил причину (mise не активирован без .mise.toml), предложил
> правильное решение (глобальный node), и сразу выполнил его.

---

## Создание структуры проекта

```
как правильно выстроить процесс чтобы использовать настройки окружения
из ai-setup в своих проектах?
```

> Агент объяснил концепцию двух уровней (глобальный mise + проектный .mise.toml)
> и предложил конкретный план действий.

---

```
давай сделаем шаг 1 и создадим тут
/home/aromanychev/edu/ai-setup/homeworks/hw-0/project-pattern
шаблон для новых проектов
```

> Агент изучил структуру ai-setup, создал шаблон из 4 файлов:
> .mise.toml, CLAUDE.md, Makefile, .gitignore.
